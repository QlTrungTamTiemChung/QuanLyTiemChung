@model QLTrungTamTiemChung.Areas.Admin.Models.EntityFramework.NhaCungCap

@{
    ViewBag.Title = "Chỉnh sửa";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

@if (TempData["thongbao"] != null)
{
    <div class="alert alert-success alert-dismissible" role="alert">
        <strong>@TempData["thongbao"]</strong>
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>
}
<div class="row-fluid sortable">
    <div class="box span12">
        <div class="box-header" data-original-title>
            <h2><i class="halflings-icon edit"></i><span class="break"></span>Sửa nhà cung cấp</h2>
        </div>
        <div class="box-content">
            @using (Html.BeginForm("Update", "NhaCungCap", FormMethod.Post, new { @class = "form-horizontal" }))
            {
                @Html.AntiForgeryToken()
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })

                <input name="MaNCC" type="text" style="display:none" value="@Model.MaNCC" />

                <div class="control-group">
                    <label class="control-label" for="TenNCC">Tên nhà cung cấp</label>
                    <div class="controls">
                        <input class="input-xlarge focused" name="TenNCC" id="TenNCC" type="text" value="@Model.TenNCC">
                    </div>
                </div>

                <div class="control-group">
                    <label class="control-label" for="DiaChi">Địa chỉ</label>
                    <div class="controls">
                        <input class="input-xlarge focused" name="DiaChi" id="DiaChi" type="text" value="@Model.DiaChi">
                    </div>
                </div>

                <div class="control-group">
                    <label class="control-label" for="SDT">Điện thoại</label>
                    <div class="controls">
                        <input class="input-xlarge focused" name="SDT" id="SDT" type="text" value="@Model.SDT">
                    </div>
                </div>

                <div class="control-group">
                    <div class="controls">
                        <input type="submit" value="Cập nhật" class="btn btn-success" />
                    </div>
                </div>
            }
        </div>
    </div><!--/span-->

</div><!--/row-->
